<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.com.hwool.hermitageintelligenceagency">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_custom_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="au.com.hermitagewool.views.CustomiseQuiltActivity">
        </activity>

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity
            android:name="au.com.hermitagewool.views.MainActivity"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="au.com.hermitagewool.views.ScanActivity" />

        <activity
            android:name="au.com.hermitagewool.views.NewsActivity"
            android:theme="@style/AppTheme" />

        <activity android:name="au.com.hermitagewool.views.AddressDetailsActivity">
        </activity>
    </application>

</manifest>